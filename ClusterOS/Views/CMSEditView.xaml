<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ClusterOS.Views.CMSEditView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClusterOS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="20">
        <StackPanel Spacing="15">
            <TextBlock Text="Edit Post" FontSize="24" FontWeight="SemiBold"/>

            <TextBlock Text="Category:"/>
            <ComboBox x:Name="cmbEditCategory" PlaceholderText="Select category" SelectionChanged="cmbEditCategory_SelectionChanged">
                <ComboBoxItem Content="Category 1" />
                <ComboBoxItem Content="Category 2" />
            </ComboBox>

            <ListView x:Name="lvPosts" Margin="0,20">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Post">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <TextBlock Text="{x:Bind Title}" FontWeight="Bold" Margin="0,6,0,0"/>
                            <Button Content="Edit" Click="EditPost_Click"/>
                            <Button Content="Remove" Click="RemovePost_Click"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>

</Page>
