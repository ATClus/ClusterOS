<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ClusterOS.Views.CMSCreateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClusterOS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0">
            <StackPanel Spacing="15">
                <TextBlock Text="Create Post" FontSize="24" FontWeight="SemiBold"/>

                <TextBlock Text="Title:"/>
                <TextBox x:Name="txtTitle" PlaceholderText="Enter title"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="5">
                        <TextBlock Text="Category:"/>
                        <ComboBox x:Name="cmbCategory" PlaceholderText="Select category"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Spacing="5">
                        <TextBlock Text="Highlight:" VerticalAlignment="Center"/>
                        <ToggleSwitch x:Name="tglHighlight" OffContent="False" OnContent="True" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Spacing="5">
                        <TextBlock Text="Author:"/>
                        <TextBox x:Name="txtAuthor" PlaceholderText="Post author"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Summary:"/>
                <TextBox x:Name="txtSummary" PlaceholderText="Post summary" AcceptsReturn="True" Height="80"/>

                <TextBlock Text="Content:"/>
                <Grid ColumnSpacing="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Spacing="5">
                        <TextBlock Text="Portuguese:" FontWeight="Bold"/>
                        <TextBox x:Name="txtContentPT" PlaceholderText="Content in Portuguese" AcceptsReturn="True" Height="120"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Spacing="5">
                        <TextBlock Text="English:" FontWeight="Bold"/>
                        <TextBox x:Name="txtContentEN" PlaceholderText="Content in English" AcceptsReturn="True" Height="120"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="1" 
                Content="Save Post" 
                Click="SavePost_Click" 
                HorizontalAlignment="Center" 
                Margin="0,20,0,0"
                Background="#0078D4"
                Foreground="White"/>
    </Grid>
</Page>
