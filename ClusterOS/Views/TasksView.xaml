<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ClusterOS.Views.TasksView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClusterOS.Views"
    xmlns:model="using:ClusterOS.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <SelectorBar x:Name="TasksSelectorBar" SelectionChanged="TasksSelectorBar_SelectionChanged">
            <SelectorBarItem x:Name="DisplayTasksItem" Text="Tasks" IsSelected="True"/>
            <SelectorBarItem x:Name="CreateEditTaskItem" Text="Create Task"/>
        </SelectorBar>

        <Frame x:Name="TasksRoot" Grid.Row="1"/>
    </Grid>
</Page>
