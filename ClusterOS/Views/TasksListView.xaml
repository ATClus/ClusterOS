<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ClusterOS.Views.TasksListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClusterOS.Views"
    xmlns:model="using:ClusterOS.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Tasks List"
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="0,0,0,20"/>
            <Grid Width="1300" Padding="10" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="500"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="ID" FontWeight="Bold"/>
                <TextBlock Grid.Column="1" Text="Title" FontWeight="Bold"/>
                <TextBlock Grid.Column="2" Text="Description" FontWeight="Bold"/>
                <TextBlock Grid.Column="3" Text="Priority" FontWeight="Bold"/>
                <TextBlock Grid.Column="4" Text="Created At" FontWeight="Bold"/>
                <TextBlock Grid.Column="5" Text="Updated At" FontWeight="Bold"/>
                <TextBlock Grid.Column="6" Text="Actions" FontWeight="Bold"/>
            </Grid>
        </StackPanel>

        <ListView Grid.Row="1"
                  x:Name="TasksListViewPresentation"
                  ItemsSource="{x:Bind Tasks, Mode=OneWay}"
                  IsItemClickEnabled="False">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:TaskItem">
                    <Grid Width="1300" Padding="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="500"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{x:Bind id, Mode=OneWay}"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind title, Mode=OneWay}"/>
                        <TextBlock Grid.Column="2" Text="{x:Bind description, Mode=OneWay}"/>
                        <TextBlock Grid.Column="3" Text="{x:Bind priority, Mode=OneWay}"/>
                        <TextBlock Grid.Column="4" Text="{x:Bind createdAt, Mode=OneWay}"/>
                        <TextBlock Grid.Column="5" Text="{x:Bind updatedAt, Mode=OneWay}"/>

                        <StackPanel Grid.Column="6" Orientation="Horizontal">
                            <Button Content="Edit"
                                    Click="EditTask_Click"
                                    Tag="{x:Bind id, Mode=OneWay}"
                                    Margin="0,0,5,0"
                                    Background="#0078D4"
                                    Foreground="White"/>
                            <Button Content="Delete"
                                    Click="DeleteTask_Click"
                                    Tag="{x:Bind id, Mode=OneWay}"
                                    Background="#0078D4"
                                    Foreground="White"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
