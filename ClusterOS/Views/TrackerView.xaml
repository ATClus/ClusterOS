<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ClusterOS.Views.TrackerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClusterOS.Views"
    xmlns:model="using:ClusterOS.Models"
    xmlns:helper="using:ClusterOS.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <helper:DateTimeToStringConverter x:Key="DateTimeToStringConverter"/>
        <helper:TimeSpanToStringConverter x:Key="TimeSpanToStringConverter"/>
    </Page.Resources>

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Spacing="12" Margin="0,0,0,12">
            <TextBlock Text="App Title:" VerticalAlignment="Center"/>
            <TextBox x:Name="AppTitleFilterTextBox" Width="200" TextChanged="FilterTextBox_TextChanged"/>
            <TextBlock Text="Tab URL:" VerticalAlignment="Center"/>
            <TextBox x:Name="TabUrlFilterTextBox" Width="200" TextChanged="FilterTextBox_TextChanged"/>
            <TextBlock Text="Date:" VerticalAlignment="Center"/>
            <DatePicker x:Name="DateFilterPicker" Width="150" DateChanged="DateFilterPicker_DateChanged"/>
            <Button Content="Clear" 
                    Background="#0078D4"
                    Foreground="White"
                    Click="ClearFiltersButton_Click"/>
        </StackPanel>

        <Grid Grid.Row="1" Padding="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="ID" Grid.Column="0" Margin="4" FontWeight="Bold"/>
            <TextBlock Text="App Title" Grid.Column="1" Margin="4" FontWeight="Bold"/>
            <TextBlock Text="Tab URL" Grid.Column="2" Margin="4" FontWeight="Bold"/>
            <TextBlock Text="Date" Grid.Column="3" Margin="4" FontWeight="Bold"/>
            <TextBlock Text="Duration" Grid.Column="4" Margin="4" FontWeight="Bold"/>
        </Grid>

        <ListView x:Name="TrackDataListView" Grid.Row="2">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:TrackDataItemModel">
                    <Grid Padding="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind Id}" Grid.Column="0" Margin="4"/>
                        <TextBlock Text="{x:Bind AppTitle}" Grid.Column="1" Margin="4"/>
                        <TextBlock Text="{x:Bind TabUrl}" Grid.Column="2" Margin="4"/>
                        <TextBlock Text="{x:Bind Date, Converter={StaticResource DateTimeToStringConverter}}" Grid.Column="3" Margin="4"/>
                        <TextBlock Text="{x:Bind Duration, Converter={StaticResource TimeSpanToStringConverter}}" Grid.Column="4" Margin="4"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
